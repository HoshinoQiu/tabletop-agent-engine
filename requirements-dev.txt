# Dev/test dependencies (install with: pip install -r requirements-dev.txt)
-r requirements.txt
pytest>=7.4.0
pytest-asyncio>=0.21.0
# Keep compatible with FastAPI 0.104 / Starlette 0.27 TestClient
httpx>=0.25.0,<0.28.0
